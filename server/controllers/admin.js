const app = require('./../index');

function payDealer(req, res) {

}

// an admin can send out an email to someone that when clicked,
// allows the user to create an account and automatically have cashier priviledges
function sendCashierInviteEmail(req, res) {

}

module.exports = {
    payDealer,
    sendCashierInviteEmail,
}